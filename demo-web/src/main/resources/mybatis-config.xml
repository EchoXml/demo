<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 配置全局属性 -->
	<settings>
		<!-- 使用jdbc的getGeneratedKeys获取数据库自增主键值 -->
		<setting name="useGeneratedKeys" value="true" />

		<!-- 使用列别名替换列名 默认:true -->
		<setting name="useColumnLabel" value="true" />

		<!-- 开启驼峰命名转换:Table{create_time} -> Entity{createTime} -->
		<setting name="mapUnderscoreToCamelCase" value="true" />
	</settings>
	<!-- 配置别名 -->
	<typeAliases>
		<typeAlias alias="result" type="com.echo.dto.Result" />
		<typeAlias alias="book" type="com.echo.model.Book" />
		<typeAlias alias="userinfo" type="com.echo.model.UserInfo" />
		<typeAlias alias="appointment" type="com.echo.model.Appointment" />
		<typeAlias alias="bookrec" type="com.echo.model.BookRec" />
	</typeAliases>
</configuration>